__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.pytest_cache/

# generated corpus — reproducible from `cast generate --seed 1337`
data/*.jsonl
data/manifest.json

# training artifacts
runs/*/ckpt.pt
runs_*.log
*.pt

# The NEDB ledger IS shipped — it is 4KB, portable, and lets anyone who clones
# run `cast lineage` and verify provenance without training anything.
# (deliberately NOT ignored)
